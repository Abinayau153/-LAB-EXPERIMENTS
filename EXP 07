#include<stdio.h>

int main()
{
    int n,i,j,temp;

    printf("Enter number of processes: ");
    scanf("%d",&n);

    int bt[n];

    for(i=0;i<n;i++)
    {
        printf("Burst Time: ");
        scanf("%d",&bt[i]);
    }

    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(bt[i]>bt[j])
            {
                temp=bt[i];
                bt[i]=bt[j];
                bt[j]=temp;
            }
        }
    }

    printf("\nExecution Order\n");

    for(i=0;i<n;i++)
        printf("%d ",bt[i]);

    return 0;
}
